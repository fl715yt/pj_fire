pj_fire/
│
├── db/
│   ├── pj_fire.db          # SQLite database file (auto-created later)
│   └── schema_migration.py # Script to create tables if missing
│
├── screening/
│   ├── fetcher.py          # Scraper / API fetching logic
│   ├── screener.py         # Pre-filter, candidate selection, scoring
│   ├── categorizer.py      # Categorize stock drop reasons
│   └── reasoning.py        # GPT-based reasoning generation
│
├── utils/
│   ├── db_handler.py       # Database connection and CRUD helpers
│   ├── logger.py           # Save daily run logs
│   └── config.py           # Settings (thresholds, weights, API keys)
│
├── line_bot/
│   ├── line_sender.py      # Send daily signals to LINE
│   └── line_templates.py   # Message templates (future use)
│
├── logs/
│   └── (generated daily logs here)
│
├── .env                    # Environment variables (API keys, LINE tokens)
├── .gitignore              # Ignore venv/, db/, .env, etc.
├── requirements.txt        # Installed Python packages
├── README.md               # Project overview
└── main.py                 # Main runner to trigger daily process
